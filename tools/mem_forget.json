{
  "name": "mem_forget",
  "description": "Soft-delete a memory by ID",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Memory ID to delete (required)"
      }
    },
    "required": ["id"]
  }
}
