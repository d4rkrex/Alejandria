# Path to Alejandria MCP server binary
# Build with: cargo build --release --bin alejandria
ALEJANDRIA_BIN=./target/release/alejandria

# Path to Alejandria database (optional, uses default if not set)
# The database will be created if it doesn't exist
ALEJANDRIA_DB=~/.alejandria/memories.db
